# MongoDB connection URI (replace with your own cluster URI)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/foodrepo?retryWrites=true&w=majority

# Port your Next.js app will run on (optional, default is 3000)
PORT=3000

# Optional: Node environment
NODE_ENV=development
